# 20.01 Runtime and Connectors

The runtime and connector crates turn source-specific knowledge access into
recoverable, deduplicated, provenance-aware sync operations.

## Connector Contract

Connectors implement the shared source contract:

- `source_id` identifies the source instance;
- `source_kind` declares source class such as Wikipedia, PubMed, or customer
  private corpus;
- `capabilities` describes discover, fetch, delta, live-query, and license
  behavior;
- `discover`, `fetch`, and `delta` provide source-specific access paths.

The connector boundary is deliberately source-aware. A PubMed connector should
surface PMID, DOI, MeSH, and publication metadata. A customer corpus connector
should preserve tenant and ACL-relevant metadata. A Wikipedia connector should
preserve page and revision identity when available.

## Runtime Contract

`wendao-nexus-runtime` owns the orchestration layer:

- job registry facade;
- checkpoint registry facade;
- content hash registry for dedup;
- raw-to-normalized document normalization boundary;
- local mirror query facade for normalized documents;
- sync runtime that records pending, running, succeeded, failed, skipped,
  cached, and deduped states;
- deterministic in-memory registry and knowledge store for tests and early
  embedding.

Durable stores such as DuckDB, LanceDB, Valkey, object storage, or graph indexes
should attach behind runtime traits or future storage crates. They should not
enter `wendao-nexus-core`.

## Normalization Contract

`KnowledgeDocumentNormalizer` turns a fetched `RawSourceDocument` into an
`ExternalKnowledgeDocument`. The runtime ships a deterministic
`PlainTextNormalizer` for early mirror tests and simple customer corpus
ingestion.

Attachment and binary document parsing stays on the Rust Wendao side. Wendao
connects Nexus, schedules document extraction, calls its Docling-backed
attachment pipeline, and then routes parsed evidence into agent/index flows.
Nexus only keeps extraction resource shapes as protocol-level handoff contracts
when a consumer needs to describe Wendao extraction output without importing
`xiuxian-*` crates.

Source-specific normalizers for PubMed XML, Wikipedia HTML, PDF, or customer
JSON can implement the same trait without changing connector or query
contracts.

`NexusSyncRuntime::ingest_once` composes:

- connector fetch;
- content hash dedup;
- normalization;
- local mirror upsert;
- checkpoint update with last content hash and revision metadata.

`NexusSyncRuntime::upsert_normalized_document` is the Wendao-side handoff path
for already parsed evidence. Rust Wendao can run attachment/Docling extraction,
build an `ExternalKnowledgeDocument`, and ask Nexus to record the mirror write,
content hash dedup signal, and checkpoint without giving Nexus ownership of the
parser or agent scheduler. Nexus validates that document identity, canonical URI,
provenance, and content hash agree before accepting the handoff.

The `wendao-nexus` facade crate can mount this local mirror through
`LocalMirrorFlightHandler`, which adapts `LocalKnowledgeStore` plus
`CheckpointRegistry` to `NexusFlightCommandHandler` without starting a Nexus
server.

## Local Query Contract

`LocalKnowledgeStore` is the first runtime query facade. It supports:

- upserting normalized `ExternalKnowledgeDocument` records;
- opening one document by `source_id` and `external_id`;
- listing local mirror documents by source;
- searching local evidence with source, trust, freshness, and limit filters.

The in-memory implementation is deterministic and intended for tests, fake
connectors, and early Wendao embedding. Production backends should implement the
same trait over DuckDB, LanceDB, Arrow, Valkey-backed hot state, or a composite
store.

## Current Connector Slice

The skeleton includes:

- `StaticKnowledgeConnector` for deterministic tests and early protocol proof;
- `WikipediaConnector` as a typed unsupported-live stub;
- `PubMedConnector` as a typed unsupported-live stub;
- `CustomerCorpusConnector` as the private corpus boundary stub.

Live API behavior should be added only with source-specific rate limit, retry,
auth, fixture, and provenance tests.

## Two Access Modes

Nexus should support both local mirror and federated live query modes. The
default query path should prefer local cached evidence when it is fresh enough,
then call live sources when freshness, authority, or user intent requires it.
Live results should still write back provenance, content hash, and registry
state when policy allows.
